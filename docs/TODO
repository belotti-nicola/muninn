- aggiungere len ai messaggi può aiutare.
- gzip
- arena allocator instead of a Q. 
- cicli vita file
- buffer too lungo
- stack smashing seg faul when bounds in Q are not met
- 
  logger.info(...);   // batch
  logger.debug(...);  // batch
  logger.fatal(...);  // flush + fsync
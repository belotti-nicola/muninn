1.0.0 :: C a file logger
2.0.0 :: Compressing file when 1MB is reached
2.0.1 :: Better sizing of the two used Qs
2.1.0 :: severity level + flush policy 
2.2.0 :: Ring buffer in logger
2.2.1 :: fix to lz4 compression / panic dump support

2.2.2 :: Missed an opportunity :(

3.0.0 :: Console logger
3.1.0 :: Configurable threads working
3.1.1 :: Fix Queue dimenion in muninn struct
3.2.0 :: Better buffer handling in worker threads

Currently doing:
3.2.1 :: A FIX for the meme
3.3.0 :: Configure messages dump / log


Further developments
Double buffer Qs
Removing con wait to implement LOSSY (flags needed)
    